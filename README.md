# fmge-app

Static **mockup** of the FMGE Coach app — a calm, coach-first practice-exam web app (login → timed
tests → instant scored review). Single self-contained `index.html`, no build step, served free via
GitHub Pages. Anonymised (no personal data). Design/plan lives in the private `fmge-study` repo.
